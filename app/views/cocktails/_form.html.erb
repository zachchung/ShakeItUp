<%= simple_form_for(@cocktail) do |f| %>
  <%= f.input :name %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%#= f.input :photo, as: :file %> <%# allow one photo upload %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
